# Production build env vars — only loaded by `vite build`.
# Local dev (`vite dev`) uses .env which still has the dev/test Clerk
# key, so we never accidentally talk to the live Clerk instance from
# localhost.
VITE_CLERK_PUBLISHABLE_KEY=pk_live_Y2xlcmsubGF1bmNoZmx5LmlvJA
